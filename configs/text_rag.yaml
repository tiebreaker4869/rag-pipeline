# Text-only RAG Pipeline Configuration
# Uses TextRAGPipeline with markdown files

pipeline:
  type: text_rag
  params:
    # doc_dir will be overridden for each document
    doc_dir: ""

    # Chunking parameters
    chunk_size: 512
    chunk_overlap: 128

    # Retrieval parameters
    top_k: 10

    # Model selection
    llm_model: gemini-2.5-flash
    embedding_model: BAAI/bge-large-en-v1.5

    # Reranker (optional)
    use_reranker: true
    reranker_model: BAAI/bge-reranker-base
    rerank_top_k: 5  # defaults to top_k if null

# Inference settings
inference:
  # Path to save metrics (optional)
  metrics_output: output/text_rag_metrics.csv